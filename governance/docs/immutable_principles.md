# Immutable Principles

These principles cannot be changed:

- Core consensus cannot be bypassed
- Audit logging cannot be disabled
- Kill switch must always remain callable
- State rollback must remain available

Any attempt to violate these principles must automatically escalate to CRITICAL.
